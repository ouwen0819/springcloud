server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    #fetch-registry: true
    service-url:
      #设置与交互的地址查询服务和注册服务都需要依赖这个地址
      defaultZone: http://eureka7001.com:7001/eureka/

feign:
  hystrix:
    enabled: true